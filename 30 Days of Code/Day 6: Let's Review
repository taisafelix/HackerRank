# Task: given print its even-indexed and odd-indexed characters as  
# space-separated strings on a single line 

for i in range(int(input())): 
    s=input(); 
    print(*["".join(s[::2]),"".join(s[1::2])])
